"""Draft-pick assistant: screenshot of an in-progress draft -> next-pick recommendation.

Ported from the original mtg_draft_assistant. Not the current focus (see `sealed`), but kept
working atop the shared `core` to keep the toolkit general.
"""
